import React from 'react'
import ReactDOM from 'react-dom/client'
import DupeshopLandingPage from '../dupeshop_za_landing_page'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DupeshopLandingPage />
  </React.StrictMode>,
)
